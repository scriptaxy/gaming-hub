name: Build iOS App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-ios:
    runs-on: macos-15
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Install iOS Workload (specific version for Xcode 16)
        run: |
          dotnet workload install ios --version 9.0.100

      - name: Restore NuGet packages
        run: dotnet restore "gaming hub/gaming hub.csproj"

      - name: Build for iOS Simulator
        run: dotnet build "gaming hub/gaming hub.csproj" -c Debug -f net9.0-ios -r iossimulator-arm64 --no-restore

      - name: Build for iOS Device
        run: dotnet build "gaming hub/gaming hub.csproj" -c Release -f net9.0-ios -r ios-arm64 --no-restore
        continue-on-error: true

      - name: List build outputs
        run: |
          echo "=== Simulator Build ==="
          find "gaming hub/bin/Debug" -name "*.app" 2>/dev/null || echo "No simulator app found"
          echo "=== Release Build ==="
          find "gaming hub/bin/Release" -name "*.app" 2>/dev/null || echo "No release build found"

      - name: Upload Simulator Build
        uses: actions/upload-artifact@v4
        with:
          name: ios-simulator-build
          path: gaming hub/bin/Debug/net9.0-ios/iossimulator-arm64/
          retention-days: 14
          if-no-files-found: warn

      - name: Upload Release Build
        uses: actions/upload-artifact@v4
        with:
          name: ios-device-build
          path: gaming hub/bin/Release/net9.0-ios/ios-arm64/
          retention-days: 14
          if-no-files-found: warn

  build-info:
    runs-on: macos-14
    needs: build-ios
    if: github.event_name != 'pull_request'
    
    steps:
      - name: Build Info
        run: |
          echo "============================================"
          echo "  Gaming Hub iOS Build Complete"
          echo "============================================"
          echo ""
          echo "To install on iPhone:"
          echo "1. Download artifacts from this workflow"
          echo "2. Use Sideloadly or AltStore"
          echo "3. Sign with your Apple ID"
          echo ""
          echo "============================================"
