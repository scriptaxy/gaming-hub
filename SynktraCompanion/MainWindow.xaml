<Window x:Class="SynktraCompanion.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Synktra Companion" Height="680" Width="1000"
      MinHeight="550" MinWidth="800" WindowStartupLocation="CenterScreen"
      Background="{StaticResource BgBrush}" ResizeMode="CanResizeWithGrip"
   WindowStyle="None" AllowsTransparency="False"
     Icon="app.ico">
 <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="40" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>
    <Grid>
   <Grid.RowDefinitions>
    <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

     <Grid Grid.Row="0" Background="{StaticResource SidebarBrush}">
         <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
    <TextBlock Text="SYNKTRA" FontSize="13" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
  <TextBlock Text=" COMPANION" FontSize="13" FontWeight="Light" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
 </StackPanel>
  <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
     <Button x:Name="MinBtn" Content="&#xE949;" FontFamily="Segoe MDL2 Assets" FontSize="10" Width="46" Height="40" Click="MinBtn_Click">
      <Button.Style>
       <Style TargetType="Button">
             <Setter Property="Background" Value="Transparent"/>
     <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
      <Setter.Value>
            <ControlTemplate TargetType="Button">
        <Border x:Name="bg" Background="{TemplateBinding Background}">
       <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
 </Border>
      <ControlTemplate.Triggers>
    <Trigger Property="IsMouseOver" Value="True">
     <Setter TargetName="bg" Property="Background" Value="{StaticResource CardBrush}"/>
        </Trigger>
     </ControlTemplate.Triggers>
               </ControlTemplate>
     </Setter.Value>
         </Setter>
            </Style>
        </Button.Style>
         </Button>
              <Button x:Name="MaxBtn" Content="&#xE739;" FontFamily="Segoe MDL2 Assets" FontSize="10" Width="46" Height="40" Click="MaxBtn_Click">
     <Button.Style>
          <Style TargetType="Button">
                 <Setter Property="Background" Value="Transparent"/>
 <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
     <Setter Property="BorderThickness" Value="0"/>
         <Setter Property="Template">
          <Setter.Value>
     <ControlTemplate TargetType="Button">
           <Border x:Name="bg" Background="{TemplateBinding Background}">
        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
        <ControlTemplate.Triggers>
       <Trigger Property="IsMouseOver" Value="True">
       <Setter TargetName="bg" Property="Background" Value="{StaticResource CardBrush}"/>
               </Trigger>
       </ControlTemplate.Triggers>
               </ControlTemplate>
 </Setter.Value>
         </Setter>
       </Style>
             </Button.Style>
            </Button>
         <Button x:Name="CloseBtn" Content="&#xE106;" FontFamily="Segoe MDL2 Assets" FontSize="10" Width="46" Height="40" Click="CloseBtn_Click">
 <Button.Style>
           <Style TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
         <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
                 <ControlTemplate TargetType="Button">
    <Border x:Name="bg" Background="{TemplateBinding Background}">
  <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
   <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
        <Setter TargetName="bg" Property="Background" Value="{StaticResource DangerBrush}"/>
         <Setter Property="Foreground" Value="White"/>
          </Trigger>
</ControlTemplate.Triggers>
    </ControlTemplate>
                </Setter.Value>
       </Setter>
  </Style>
          </Button.Style>
                </Button>
         </StackPanel>
    </Grid>

        <Grid Grid.Row="1">
          <Grid.ColumnDefinitions>
          <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
       </Grid.ColumnDefinitions>
            
    <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
     <DockPanel>
     <Border DockPanel.Dock="Bottom" Margin="8,8" Padding="12" CornerRadius="8" Background="{StaticResource CardBrush}">
   <StackPanel>
 <StackPanel Orientation="Horizontal">
      <Ellipse x:Name="StatusIndicator" Width="8" Height="8" Fill="{StaticResource SuccessBrush}" Margin="0,0,8,0"/>
  <TextBlock x:Name="StatusText" Text="Running" Foreground="{StaticResource TextBrush}" FontSize="12" FontWeight="Medium"/>
         </StackPanel>
<TextBlock x:Name="IpAddressText" Text="localhost:5000" Foreground="{StaticResource TextMutedBrush}" FontSize="10" Margin="16,4,0,0"/>
    </StackPanel>
            </Border>
        <StackPanel Margin="0,16,0,8">
        <TextBlock Text="NAVIGATION" FontSize="10" Foreground="{StaticResource TextMutedBrush}" Margin="20,0,0,8" FontWeight="SemiBold"/>
      <RadioButton x:Name="NavDashboard" Content="Dashboard" IsChecked="True" Click="NavButton_Click" Tag="Dashboard" Style="{StaticResource NavButton}"/>
    <RadioButton x:Name="NavGames" Content="Games Library" Click="NavButton_Click" Tag="Games" Style="{StaticResource NavButton}"/>
     <RadioButton x:Name="NavAccounts" Content="Platforms" Click="NavButton_Click" Tag="Accounts" Style="{StaticResource NavButton}"/>
   <RadioButton x:Name="NavSettings" Content="Settings" Click="NavButton_Click" Tag="Settings" Style="{StaticResource NavButton}"/>
        </StackPanel>
       </DockPanel>
    </Border>
            
     <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
           <Grid Margin="28,24">
              <StackPanel x:Name="DashboardPage">
    <TextBlock Text="Dashboard" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,24"/>
        <UniformGrid Columns="4" Margin="0,0,0,24">
    <Border Style="{StaticResource Card}" Margin="0,0,10,0">
  <StackPanel>
       <TextBlock Text="CPU" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Medium"/>
         <TextBlock x:Name="CpuUsageText" Text="0%" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,8,0,0"/>
    <ProgressBar Value="0" Maximum="100" Height="4" Margin="0,8,0,0" Foreground="{StaticResource AccentBrush}" Background="{StaticResource BorderBrush}"/>
               </StackPanel>
</Border>
   <Border Style="{StaticResource Card}" Margin="5,0">
             <StackPanel>
           <TextBlock Text="MEMORY" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Medium"/>
            <TextBlock x:Name="MemoryUsageText" Text="0%" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,8,0,0"/>
       <ProgressBar Value="0" Maximum="100" Height="4" Margin="0,8,0,0" Foreground="{StaticResource PrimaryBrush}" Background="{StaticResource BorderBrush}"/>
</StackPanel>
    </Border>
<Border Style="{StaticResource Card}" Margin="5,0">
       <StackPanel>
          <TextBlock Text="GPU" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Medium"/>
  <TextBlock x:Name="GpuUsageText" Text="-" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,8,0,0"/>
             <ProgressBar Value="0" Maximum="100" Height="4" Margin="0,8,0,0" Foreground="{StaticResource SuccessBrush}" Background="{StaticResource BorderBrush}"/>
       </StackPanel>
               </Border>
        <Border Style="{StaticResource Card}" Margin="10,0,0,0">
      <StackPanel>
        <TextBlock Text="GAMES" Foreground="{StaticResource TextMutedBrush}" FontSize="11" FontWeight="Medium"/>
       <TextBlock x:Name="GamesCountText" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,8,0,0"/>
   <TextBlock Text="Installed" Foreground="{StaticResource TextMutedBrush}" FontSize="10" Margin="0,8,0,0"/>
       </StackPanel>
 </Border>
          </UniformGrid>
         <Border Style="{StaticResource Card}">
              <StackPanel>
    <TextBlock Text="Currently Playing" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,12"/>
          <Border Background="{StaticResource InputBgBrush}" CornerRadius="6" Padding="12">
             <TextBlock x:Name="CurrentGameText" Text="No game running" Foreground="{StaticResource TextMutedBrush}" FontSize="13"/>
           </Border>
              </StackPanel>
    </Border>
         </StackPanel>
      
  <StackPanel x:Name="GamesPage" Visibility="Collapsed">
       <DockPanel Margin="0,0,0,20">
        <TextBlock Text="Games Library" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
         <Button Content="Scan Games" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Click="ScanGames_Click"/>
            </DockPanel>
              <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
      <RadioButton x:Name="FilterAll" Content="All" IsChecked="True" Style="{StaticResource FilterButton}" Click="Filter_Click"/>
                   <RadioButton x:Name="FilterSteam" Content="Steam" Style="{StaticResource FilterButton}" Click="Filter_Click"/>
           <RadioButton x:Name="FilterEpic" Content="Epic" Style="{StaticResource FilterButton}" Click="Filter_Click"/>
       <RadioButton x:Name="FilterGOG" Content="GOG" Style="{StaticResource FilterButton}" Click="Filter_Click"/>
    <RadioButton x:Name="FilterOther" Content="Other" Style="{StaticResource FilterButton}" Click="Filter_Click"/>
      </StackPanel>
     <ItemsControl x:Name="GamesListBox">
         <ItemsControl.ItemsPanel>
     <ItemsPanelTemplate>
      <WrapPanel/>
    </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
            <DataTemplate>
  <Border Background="{StaticResource CardBrush}" CornerRadius="10" Width="180" Height="220" Margin="0,0,12,12" Cursor="Hand">
        <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
    <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
              <Border Grid.Row="0" CornerRadius="10,10,0,0" Background="{StaticResource InputBgBrush}">
 <TextBlock Text="G" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource TextMutedBrush}" FontWeight="Bold"/>
    </Border>
      <StackPanel Grid.Row="1" Margin="12,12,12,8">
         <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis"/>
 <TextBlock Text="{Binding Platform}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="0,4,0,0"/>
               <Button Content="Launch" Style="{StaticResource PrimaryButton}" Margin="0,8,0,0" Padding="8,6" FontSize="11" Click="LaunchGame_Click" Tag="{Binding}"/>
    </StackPanel>
          </Grid>
             </Border>
   </DataTemplate>
          </ItemsControl.ItemTemplate>
 </ItemsControl>
   </StackPanel>
  
             <StackPanel x:Name="AccountsPage" Visibility="Collapsed">
      <TextBlock Text="Gaming Platforms" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,24"/>
          <Border Style="{StaticResource Card}" Margin="0,0,0,10">
        <Grid>
       <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
     <ColumnDefinition Width="*"/>
<ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
     <Border Width="48" Height="48" CornerRadius="8" Background="#1B2838">
                <TextBlock Text="S" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
               </Border>
   <StackPanel Grid.Column="1" Margin="16,0" VerticalAlignment="Center">
 <TextBlock Text="Steam" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
    <TextBlock x:Name="SteamStatusText" Text="Scanning local games" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
      </StackPanel>
  <Button Grid.Column="2" x:Name="SteamConnectBtn" Content="Open Steam" Style="{StaticResource SecondaryButton}" Click="ConnectSteam_Click"/>
                </Grid>
    </Border>
                <Border Style="{StaticResource Card}" Margin="0,0,0,10">
  <Grid>
  <Grid.ColumnDefinitions>
           <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
   <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
      <Border Width="48" Height="48" CornerRadius="8" Background="#2A2A2A">
   <TextBlock Text="E" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
           </Border>
                 <StackPanel Grid.Column="1" Margin="16,0" VerticalAlignment="Center">
         <TextBlock Text="Epic Games" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
         <TextBlock x:Name="EpicStatusText" Text="Scanning local games" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
       </StackPanel>
         <Button Grid.Column="2" x:Name="EpicConnectBtn" Content="Open Epic" Style="{StaticResource SecondaryButton}" Click="ConnectEpic_Click"/>
     </Grid>
   </Border>
   <Border Style="{StaticResource Card}">
     <Grid>
 <Grid.ColumnDefinitions>
       <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
             </Grid.ColumnDefinitions>
    <Border Width="48" Height="48" CornerRadius="8" Background="#86329B">
  <TextBlock Text="G" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
     </Border>
     <StackPanel Grid.Column="1" Margin="16,0" VerticalAlignment="Center">
       <TextBlock Text="GOG Galaxy" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}"/>
   <TextBlock x:Name="GOGStatusText" Text="Scanning local games" FontSize="11" Foreground="{StaticResource TextMutedBrush}"/>
         </StackPanel>
         <Button Grid.Column="2" x:Name="GOGConnectBtn" Content="Open GOG" Style="{StaticResource SecondaryButton}" Click="ConnectGOG_Click"/>
    </Grid>
      </Border>
             </StackPanel>
                  
    <StackPanel x:Name="SettingsPage" Visibility="Collapsed">
      <TextBlock Text="Settings" FontSize="24" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,24"/>
          <Border Style="{StaticResource Card}" Margin="0,0,0,16">
<StackPanel>
        <TextBlock Text="API SERVER" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,16"/>
       <Grid Margin="0,0,0,12">
     <Grid.ColumnDefinitions>
       <ColumnDefinition Width="120"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
      <TextBlock Text="Port" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
     <TextBox x:Name="PortTextBox" Grid.Column="1" Text="5000" MaxWidth="150" HorizontalAlignment="Left"/>
            </Grid>
    <CheckBox x:Name="StartWithWindowsCheckbox" Content="Start with Windows" Margin="0,8,0,0"/>
    <CheckBox x:Name="MinimizeToTrayCheckbox" Content="Minimize to system tray" IsChecked="True" Margin="0,8,0,0"/>
         </StackPanel>
   </Border>
            <Border Style="{StaticResource Card}" Margin="0,0,0,16">
       <StackPanel>
   <TextBlock Text="SECURITY" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,16"/>
         <Grid>
   <Grid.ColumnDefinitions>
    <ColumnDefinition Width="120"/>
 <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock Text="Auth Token" VerticalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"/>
       <TextBox x:Name="AuthTokenTextBox" Grid.Column="1"/>
   <Button Grid.Column="2" Content="Generate" Style="{StaticResource SecondaryButton}" Margin="10,0,0,0" Click="GenerateToken_Click"/>
       </Grid>
  </StackPanel>
       </Border>
           <Border Style="{StaticResource Card}">
         <StackPanel>
       <TextBlock Text="ABOUT" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" Margin="0,0,0,12"/>
        <TextBlock Text="Synktra Companion" FontSize="14" FontWeight="Medium" Foreground="{StaticResource TextBrush}"/>
        <TextBlock Text="Version 1.0.0" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
              </StackPanel>
    </Border>
        </StackPanel>
    </Grid>
         </ScrollViewer>
        </Grid>
    </Grid>
</Window>
